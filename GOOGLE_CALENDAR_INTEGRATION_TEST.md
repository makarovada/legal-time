# Инструкция по тестированию интеграции с Google Calendar

## 📋 Подготовка

### 1. Настройка Google OAuth

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Calendar API**:
   - Перейдите в "APIs & Services" → "Library"
   - Найдите "Google Calendar API" и включите его
4. Создайте OAuth 2.0 credentials:
   - Перейдите в "APIs & Services" → "Credentials"
   - Нажмите "Create Credentials" → "OAuth client ID"
   - Выберите тип "Web application"
   - Добавьте Authorized redirect URIs:
     - `http://localhost:8000/auth/google/callback` (для локальной разработки)
     - Или ваш production URL
5. Скопируйте **Client ID** и **Client Secret**

### 2. Настройка переменных окружения

Добавьте в файл `.env`:

```env
# Google OAuth настройки
GOOGLE_CLIENT_ID=ваш_client_id_здесь
GOOGLE_CLIENT_SECRET=ваш_client_secret_здесь
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# Fernet ключ для шифрования токенов (сгенерируйте новый)
FERNET_KEY=ваш_fernet_ключ_здесь
```

**Генерация Fernet ключа:**
```python
from cryptography.fernet import Fernet
key = Fernet.generate_key()
print(key.decode())  # Скопируйте это значение в FERNET_KEY
```

### 3. Применение миграций

```bash
# Создайте миграцию для новых полей
python -m alembic revision --autogenerate -m "add_google_calendar_fields"

# Примените миграцию
python -m alembic upgrade head
```

---

## 🧪 Тестирование через Swagger UI

### Шаг 1: Запуск сервера

```bash
python -m uvicorn app.main:app --reload
```

Откройте Swagger UI: http://localhost:8000/docs

### Шаг 2: Авторизация в системе

1. Используйте эндпоинт `POST /auth/login`
2. Введите email и password существующего пользователя
3. Скопируйте полученный `access_token`

### Шаг 3: Авторизация в Swagger

1. Нажмите кнопку **"Authorize"** в правом верхнем углу Swagger UI
2. Введите: `Bearer <ваш_access_token>`
3. Нажмите **"Authorize"**

---

## 🔐 Тестирование Google OAuth авторизации

### 1. Проверка статуса интеграции

**Эндпоинт:** `GET /auth/google/status`

- Должен вернуть `{"connected": false}` до авторизации

### 2. Начало авторизации Google

**Эндпоинт:** `GET /auth/google/authorize`

**Что происходит:**
- Возвращает `authorization_url`
- Скопируйте этот URL и откройте в браузере
- Войдите в Google аккаунт
- Разрешите доступ к календарю
- После авторизации вы будете перенаправлены на callback URL

**Важно:** Для локальной разработки callback URL должен быть доступен. Если используется Swagger, скопируйте `authorization_url` и откройте в браузере.

### 3. Обработка callback

**Эндпоинт:** `GET /auth/google/callback?code=...&state=...`

**Что происходит:**
- Google перенаправляет на этот URL с кодом авторизации
- Система обменивает код на токены
- Токены сохраняются в БД в зашифрованном виде

**Проверка:**
- После успешной авторизации должен вернуться JSON:
  ```json
  {
    "message": "Google Calendar successfully connected",
    "calendar_id": "primary"
  }
  ```

### 4. Проверка статуса после авторизации

**Эндпоинт:** `GET /auth/google/status`

- Теперь должен вернуть `{"connected": true, "calendar_id": "primary"}`

### 5. Отключение интеграции (опционально)

**Эндпоинт:** `POST /auth/google/disconnect`

- Удаляет сохраненные токены из БД

---

## 📅 Тестирование синхронизации таймшитов с календарем

### Предварительные требования

Убедитесь, что в БД есть:
- **Client** (клиент)
- **Contract** (договор, привязанный к клиенту)
- **Matter** (дело, привязанное к договору)
- **ActivityType** (тип активности)

### 1. Создание таймшита

**Эндпоинт:** `POST /time-entries`

**Тело запроса:**
```json
{
  "hours": 2.5,
  "description": "Консультация с клиентом",
  "date": "2025-01-15",
  "matter_id": 1,
  "activity_type_id": 1,
  "rate_id": null
}
```

**Что происходит:**
- Создается таймшит в БД
- Если Google Calendar подключен, создается событие в календаре
- В поле `google_event_id` сохраняется ID события из Google Calendar

**Проверка:**
1. Ответ должен содержать созданный таймшит с `google_event_id` (если интеграция активна)
2. Откройте Google Calendar и проверьте наличие события
3. Событие должно содержать:
   - Название: `{код_дела} - {название_дела} ({тип_активности})`
   - Описание: часы, описание, статус
   - Время: начало в 00:00 указанной даты, длительность = количество часов

### 2. Обновление таймшита

**Эндпоинт:** `PUT /time-entries/{entry_id}`

**Тело запроса:**
```json
{
  "hours": 3.0,
  "description": "Обновленное описание",
  "date": "2025-01-15",
  "matter_id": 1,
  "activity_type_id": 1,
  "rate_id": null
}
```

**Что происходит:**
- Обновляется таймшит в БД
- Если событие уже существует в календаре, оно обновляется
- Если события нет, создается новое

**Проверка:**
1. Проверьте, что событие в Google Calendar обновилось
2. Время и описание должны соответствовать новым данным

### 3. Одобрение таймшита

**Эндпоинт:** `PATCH /time-entries/{entry_id}/approve`

**Что происходит:**
- Статус таймшита меняется на "approved"
- Событие в календаре обновляется с новым статусом

**Проверка:**
- В описании события должен быть указан статус "approved"

### 4. Удаление таймшита

**Эндпоинт:** `DELETE /time-entries/{entry_id}`

**Что происходит:**
- Таймшит удаляется из БД
- Событие удаляется из Google Calendar

**Проверка:**
1. Таймшит должен быть удален из БД
2. Событие должно исчезнуть из Google Calendar

---

## 📊 Просмотр событий календаря

### Эндпоинт: `GET /time-entries/calendar/events`

**Параметры:**
- `max_results` (опционально, по умолчанию 50) - максимальное количество событий

**Что происходит:**
- Получает события из Google Calendar за последние 30 дней и следующие 30 дней
- Фильтрует только события, связанные с таймшитами (по `google_event_id`)
- Возвращает список событий с информацией о связанных таймшитах

**Ответ:**
```json
{
  "events": [
    {
      "event_id": "abc123...",
      "summary": "MAT-001 - Консультация (Консультация)",
      "description": "Часы: 2.5\nОписание: Консультация с клиентом\nСтатус: draft",
      "start": {
        "dateTime": "2025-01-15T00:00:00Z",
        "timeZone": "UTC"
      },
      "end": {
        "dateTime": "2025-01-15T02:30:00Z",
        "timeZone": "UTC"
      },
      "time_entry_id": 1,
      "status": "draft"
    }
  ],
  "total": 1
}
```

**Проверка:**
- Должны вернуться только события, созданные через систему
- Каждое событие должно быть связано с таймшитом

---

## 🔍 Проверка в базе данных

### Проверка сохранения токенов

```sql
SELECT 
    id, 
    email, 
    google_token_encrypted IS NOT NULL as has_token,
    google_refresh_token_encrypted IS NOT NULL as has_refresh_token,
    google_calendar_id
FROM employees
WHERE google_token_encrypted IS NOT NULL;
```

### Проверка связи таймшитов с событиями

```sql
SELECT 
    te.id,
    te.date,
    te.hours,
    te.google_event_id,
    m.code as matter_code,
    m.name as matter_name
FROM time_entries te
JOIN matters m ON te.matter_id = m.id
WHERE te.google_event_id IS NOT NULL;
```

---

## ⚠️ Возможные проблемы и решения

### Проблема: "Google OAuth credentials not configured"

**Решение:** Проверьте, что в `.env` файле указаны `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET`

### Проблема: "FERNET_KEY not set"

**Решение:** Сгенерируйте Fernet ключ и добавьте в `.env`:
```python
from cryptography.fernet import Fernet
print(Fernet.generate_key().decode())
```

### Проблема: "Failed to sync with Google Calendar"

**Решение:**
1. Проверьте, что пользователь авторизован в Google Calendar (`GET /auth/google/status`)
2. Проверьте, что токены не истекли (может потребоваться повторная авторизация)
3. Проверьте логи сервера для деталей ошибки

### Проблема: События не создаются в календаре

**Решение:**
1. Убедитесь, что интеграция подключена (`GET /auth/google/status` возвращает `connected: true`)
2. Проверьте, что в БД есть связанные данные (Matter, ActivityType)
3. Проверьте права доступа в Google OAuth (должны быть включены scopes для calendar.events)

### Проблема: Callback URL не работает

**Решение:**
- Для локальной разработки используйте `http://localhost:8000/auth/google/callback`
- Убедитесь, что этот URL добавлен в Authorized redirect URIs в Google Cloud Console
- Для тестирования можно временно использовать `http://127.0.0.1:8000/auth/google/callback`

---

## ✅ Чеклист тестирования

- [ ] Google OAuth credentials настроены
- [ ] Переменные окружения заполнены
- [ ] Миграции применены
- [ ] Пользователь авторизован в системе (JWT токен получен)
- [ ] Google Calendar интеграция подключена (`/auth/google/authorize` → callback)
- [ ] Статус интеграции показывает `connected: true`
- [ ] Создан таймшит и событие появилось в Google Calendar
- [ ] Обновлен таймшит и событие обновилось в календаре
- [ ] Таймшит одобрен и статус обновился в событии
- [ ] Таймшит удален и событие исчезло из календаря
- [ ] Эндпоинт `/time-entries/calendar/events` возвращает список событий

---

## 📝 Дополнительные заметки

1. **Токены автоматически обновляются** при истечении срока действия (если есть refresh_token)
2. **События создаются только для авторизованных пользователей** с подключенным Google Calendar
3. **Если синхронизация не удалась**, таймшит все равно создается (ошибка логируется, но не прерывает процесс)
4. **Токены хранятся в зашифрованном виде** с использованием Fernet шифрования

---

**Готово!** Теперь вы можете протестировать всю интеграцию с Google Calendar. 🎉



